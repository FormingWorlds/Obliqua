# Using the model
Follow the [Getting started](@ref) instructions. Only read on once you have confirmed that the model runs on your machine.
Otherwise, see the [Troubleshooting](@ref) guide.

Include steps here on running the code.